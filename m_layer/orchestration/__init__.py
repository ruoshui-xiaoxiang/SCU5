# -*- coding: utf-8 -*-
"""m_layer/orchestration/ — 任务编排子包

职责：任务拆解、执行、反思、多Agent协作、并行/分布式
依赖：m_layer 根目录（llm_client/local_model/cognition）

为保持向后兼容，所有模块仍可通过 `from m_layer.xxx import` 访问，
本子包仅为逻辑分组，不改变导入路径。
"""
