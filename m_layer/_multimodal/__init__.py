# -*- coding: utf-8 -*-
"""m_layer/_multimodal/ — 多模态处理子包（逻辑分组）

职责：图像/音频/视频理解、视觉对话、语音IO
依赖：外部库（transformers/PIL/ffmpeg等），与其他子包低耦合

注意：子包名用下划线前缀 `_multimodal` 避免与现有文件 `m_layer/multimodal.py` 冲突。
模块仍在 m_layer 根目录，本子包仅为逻辑分组声明。
"""
