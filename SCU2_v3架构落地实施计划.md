# SCU3 v3 架构落地实施计划

> **文档版本**: v1.0
> **生成日期**: 2026-08-10
> **基于文档**: SCU3_架构总结报告.md
> **核心约束**: 5条不可妥协的架构原则
> **实施周期**: 4个阶段，渐进式推进

---

## 目录

1. [实施总览](#1-实施总览)
2. [5条架构原则落地映射](#2-5条架构原则落地映射)
3. [阶段1：架构基线确立](#3-阶段1架构基线确立)
4. [阶段2：核心功能补全](#4-阶段2核心功能补全)
5. [阶段3：灰度迁移验证](#5-阶段3灰度迁移验证)
6. [阶段4：生产切换](#6-阶段4生产切换)
7. [风险管控与回滚预案](#7-风险管控与回滚预案)
8. [验收标准清单](#8-验收标准清单)

---

## 1. 实施总览

### 1.1 实施路径

```
阶段1 (2周)           阶段2 (4周)          阶段3 (3周)         阶段4 (1周)
架构基线确立    →    核心功能补全    →    灰度迁移验证   →    生产切换
├─ 原则固化        →   ├─ DeepSeek接入  →   ├─ 5%流量灰度  →   ├─ 100%流量
├─ 审计模块强化    →   ├─ RAG知识库     →   ├─ 对比监控    →   ├─ 原系统下线
├─ 守卫层完善      →   ├─ 13种工具补全  →   ├─ 异常回滚    →   └─ 存档
└─ 测试框架搭建    →   ├─ 完整前端迁移  →   └─ 指标达标
                     └─ 风险修复(P0/P1)
```

### 1.2 阶段依赖关系

```
阶段1 ────── 阶段2 ────── 阶段3 ────── 阶段4
  │            │            │            │
  ├─原则固化   ├─功能补全   ├─灰度验证   ├─生产切换
  ├─审计强化   ├─风险修复   ├─监控对比   ├─原系统存档
  └─测试框架   └─集成测试   └─回滚演练   └─运维移交
       │            │            │
       └── 门槛1 ───┴── 门槛2 ───┘
       全部通过     全部通过      指标达标
```

### 1.3 总体目标

| 目标 | 衡量标准 |
|------|---------|
| 架构合规 | 5条原则全部落地，代码审计无违规 |
| 功能对等 | 与原SCU_CUF功能对等率 ≥ 95% |
| 性能达标 | P99 响应延迟 < 2s，1000 QPS稳定 |
| 安全达标 | 0个CRITICAL，0个HIGH漏洞 |
| 可运维 | 完整监控 + 告警 + 日志 + 备份 |

---

## 2. 5条架构原则落地映射

### 2.1 原则一：D层只放代码定义

> **原则内容**：运行时状态（余额/历史/哈希链/覆写表/反馈）必须在W1层，D层只放代码定义（类结构、公理常量）。

| 落地项 | 实施内容 | 责任层 | 验证方式 |
|--------|---------|--------|---------|
| D层文件清单 | 明确列出D层允许的文件：axioms.py, ledger_base.py, contracts.py | D层 | 文件清单审计 |
| D层只读校验 | 编写`d_layer_integrity_check.py`，启动时扫描D层文件，运行时定期校验哈希 | 守卫层 | 启动时 + 每小时1次 |
| W1层运行时状态 | ledger实例、_history、_balance、_hash_chain、_tax_factor_overrides、_feedback_counts 全部在W1层 | W1层 | 代码审查 |
| 禁止W2/M写D层 | firewall.py 的 A1 校验强化：任何 `target=="D" and action in ("modify","write","patch")` 直接拒绝 | 守卫层 | A1测试用例 |
| 项目硬约束更新 | 在project_memory.md中记录"D层只放代码定义，运行时状态归W1层" | 文档 | 约束检查 |

**实施代码示例**：
```python
# d_layer_integrity_check.py
import hashlib, os
D_LAYER_FILES = ["axioms.py", "ledger_base.py", "contracts.py"]
D_LAYER_HASHES = {...}  # 预计算的哈希

def verify_d_layer_integrity(d_dir: str) -> tuple[bool, str]:
    """启动时 + 定期校验D层文件完整性"""
    for fname in D_LAYER_FILES:
        fpath = os.path.join(d_dir, fname)
        if not os.path.exists(fpath):
            return False, f"D层文件缺失: {fname}"
        with open(fpath, "rb") as f:
            h = hashlib.sha256(f.read()).hexdigest()
        if h != D_LAYER_HASHES.get(fname):
            return False, f"D层文件被篡改: {fname}"
    return True, "D层完整性校验通过"
```

### 2.2 原则二：A4只管依赖方向

> **原则内容**：数据流方向不受A4约束，只受A2经济约束。A4只校验"依赖方向反向"（如D import W2）。

| 落地项 | 实施内容 | 责任层 | 验证方式 |
|--------|---------|--------|---------|
| A4校验范围限定 | firewall.py 的 `_check_a4` 只对 `action in ("import","modify","patch","base_modify")` 校验 | 守卫层 | A4测试用例 |
| 数据流动作豁免 | `query/tool_call/check/inspect/layer_jump` 等数据流动作不触发A4 | 守卫层 | 数据流测试 |
| A2经济约束补位 | 数据流跨层由A2扣税约束（W2→W1, W1→M 扣税） | 守卫层 | A2测试用例 |
| 依赖关系扫描 | 编写`dependency_scanner.py`，扫描import关系，发现D层import W2层则告警 | 构建时 | CI/CD检查 |

**实施代码示例**：
```python
# dependency_scanner.py - CI/CD 集成
import ast, os

LAYER_ORDER = {"d_layer": 0, "m_layer": 1, "w1_layer": 2, "w2_layer": 3}

def scan_dependency_violations(root_dir: str) -> list:
    """扫描import关系，发现A4违规（底层import顶层）"""
    violations = []
    for dirpath, _, filenames in os.walk(root_dir):
        layer = _detect_layer(dirpath)
        if not layer:
            continue
        for fname in filenames:
            if not fname.endswith(".py"):
                continue
            tree = ast.parse(open(os.path.join(dirpath, fname)).read())
            for node in ast.walk(tree):
                if isinstance(node, ast.ImportFrom) and node.module:
                    target_layer = _detect_layer_from_module(node.module)
                    if target_layer and LAYER_ORDER.get(layer, 0) < LAYER_ORDER.get(target_layer, 0):
                        violations.append(f"A4违规: {layer} import {target_layer} ({fname})")
    return violations
```

### 2.3 原则三：同层流动免审

> **原则内容**：W1→W1、M→M不触发守卫，只有跨CUF层才审计。

| 落地项 | 实施内容 | 责任层 | 验证方式 |
|--------|---------|--------|---------|
| 同层免审逻辑 | firewall.py 的 `check()` 方法：`if src == tgt: return True` | 守卫层 | 同层测试 |
| 业务归并确认 | 感知→W2，记忆+执行→W1，认知+元认知→M 的映射文档化 | 文档 | 代码审查 |
| 守卫点清单 | 明确3个守卫点：①W2→W1 ②W1→M ③周期审计(M→W1同层免审) | 守卫层 | 守卫点审计 |
| 免审日志记录 | 同层流动记录"same_layer_bypass=True"日志，便于审计追溯 | 守卫层 | 日志检查 |

### 2.4 原则四：工具调用独立守卫

> **原则内容**：无论同层与否，工具调用必须过ToolGuard，按read/write定税。

| 落地项 | 实施内容 | 责任层 | 验证方式 |
|--------|---------|--------|---------|
| ToolGuard独立 | tool_guard.py 独立于firewall.py，工具调用必经 | 守卫层 | 工具调用测试 |
| 动作类型映射 | 所有工具必须标注tool_type（read/write），决定税率 | W1层 | 工具注册审计 |
| 13种工具补全 | calculator/weather/time_now/text_stats/file_read/file_write/code_run/exchange_rate/crypto_price/stock_price/github_search/datetime_calc/unit_convert | W1层 | 功能测试 |
| 工具白名单 | 只读工具（file_read/weather等）首次审计后归入白名单 | 守卫层 | 白名单测试 |
| write类高税 | file_write/code_run 等写操作税率 ≥ 3.0E | D层 | 税率审计 |

### 2.5 原则五：输出必经内容过滤

> **原则内容**：系统输出前必须过ContentFilter脱敏，防止D层数据泄漏。

| 落地项 | 实施内容 | 责任层 | 验证方式 |
|--------|---------|--------|---------|
| ContentFilter必经 | server.py 的 `_build_response()` 强制调用 `content_filter.filter()` | 入口层 | 代码审查 |
| 敏感模式规则库 | 维护可更新的敏感模式规则库（正则 + 关键词） | 守卫层 | 规则库审计 |
| D层字段白名单 | 明确D层哪些字段可输出（公理名、层级名），其余脱敏 | 守卫层 | 字段白名单测试 |
| 过滤日志记录 | 所有过滤操作记录日志，便于追溯 | 守卫层 | 日志检查 |
| 定期规则更新 | 每月review敏感模式规则，补充新型泄漏模式 | 运维 | 定期审计 |

---

## 3. 阶段1：架构基线确立（2周）

### 3.1 目标

固化5条架构原则，强化审计与守卫，搭建测试框架，为阶段2功能补全奠定基础。

### 3.2 任务清单

#### 任务1.1：D层基线固化（3天）

| 子任务 | 文件 | 内容 |
|--------|------|------|
| D层文件清单 | `d_layer/MANIFEST.json` | 列出D层允许的文件及预计算哈希 |
| D层完整性校验 | `guard/d_layer_integrity.py` | 启动时+定期校验D层文件哈希 |
| A1校验强化 | `guard/firewall.py` | `target=="D" and action in ("modify","write","patch")` 直接拒绝 |
| 项目硬约束更新 | `project_memory.md` | 记录"D层只放代码定义"原则 |

**验收标准**：
- [ ] D层文件清单完整，哈希预计算
- [ ] 启动时D层完整性校验通过
- [ ] A1校验拒绝所有写D层操作
- [ ] project_memory.md 已更新

#### 任务1.2：A4公理校验强化（2天）

| 子任务 | 文件 | 内容 |
|--------|------|------|
| A4范围限定 | `guard/firewall.py` | `_check_a4` 只对 import/modify/patch/base_modify 校验 |
| 依赖关系扫描器 | `tools/dependency_scanner.py` | CI/CD集成的import关系扫描 |
| A4测试用例 | `tests/test_a4.py` | 覆盖依赖方向反向 + 数据流方向豁免 |

**验收标准**：
- [ ] A4不对数据流动作（query/tool_call/layer_jump）校验
- [ ] A4对依赖类动作（import/modify）校验
- [ ] 依赖扫描器0违规
- [ ] A4测试用例全部通过

#### 任务1.3：守卫层完善（3天）

| 子任务 | 文件 | 内容 |
|--------|------|------|
| 同层免审日志 | `guard/firewall.py` | 记录 `same_layer_bypass=True` |
| 守卫点文档 | `docs/guard_points.md` | 明确3个守卫点的触发条件 |
| ToolGuard完善 | `guard/tool_guard.py` | 支持所有13种工具的tool_type映射 |
| 工具白名单集成 | `guard/tool_guard.py` | 只读工具首次审计后归入白名单 |

**验收标准**：
- [ ] 同层流动记录免审日志
- [ ] 守卫点文档完整
- [ ] ToolGuard支持13种工具
- [ ] 只读工具白名单归档生效

#### 任务1.4：内容过滤强化（2天）

| 子任务 | 文件 | 内容 |
|--------|------|------|
| D层字段白名单 | `guard/content_filter.py` | 明确D层可输出字段 |
| 规则库扩展 | `guard/content_filter.py` | 补充内网IP/员工ID/API路径等规则 |
| 过滤日志 | `guard/content_filter.py` | 记录所有过滤操作 |
| 强制调用 | `server.py` | `_build_response()` 强制调用filter |

**验收标准**：
- [ ] D层字段白名单定义
- [ ] 规则库覆盖10+种敏感模式
- [ ] 过滤日志可追溯
- [ ] 输出必经ContentFilter

#### 任务1.5：测试框架搭建（4天）

| 子任务 | 文件 | 内容 |
|--------|------|------|
| 单元测试框架 | `tests/unit/` | 每个模块独立测试 |
| 集成测试框架 | `tests/integration/` | 端到端流程测试 |
| 安全测试框架 | `tests/security/` | 公理违规/注入/越权测试 |
| 性能测试框架 | `tests/performance/` | 并发/延迟/吞吐测试 |
| CI/CD配置 | `.github/workflows/` | 自动化测试流水线 |

**验收标准**：
- [ ] 单元测试覆盖率 ≥ 80%
- [ ] 集成测试覆盖核心流程
- [ ] 安全测试覆盖5条原则
- [ ] 性能测试框架就绪
- [ ] CI/CD流水线运行

### 3.3 阶段1门槛

| 门槛项 | 标准 | 阻塞阶段2 |
|--------|------|-----------|
| D层完整性 | 校验通过 + A1拒绝写D层 | ✅ 是 |
| A4正确性 | 数据流豁免 + 依赖校验 | ✅ 是 |
| 守卫点清晰 | 3个守卫点文档化 | ✅ 是 |
| 内容过滤 | 强制调用 + 10+规则 | ✅ 是 |
| 测试框架 | 单元+集成+安全+性能 | ✅ 是 |

---

## 4. 阶段2：核心功能补全（4周）

### 4.1 目标

补全SCU3缺失的核心功能，达到与原SCU_CUF功能对等率 ≥ 95%。

### 4.2 任务清单

#### 任务2.1：DeepSeek API接入（1周）

| 子任务 | 文件 | 内容 |
|--------|------|------|
| API客户端 | `m_layer/llm_client.py` | DeepSeek API调用封装 |
| 认知层改造 | `m_layer/cognition.py` | 改为调用LLM生成回复 |
| SSE流式响应 | `server.py` | 支持流式输出 |
| 上下文管理 | `w1_layer/memory.py` | 对话上下文管理 |
| 错误处理 | `m_layer/cognition.py` | API超时/限流/错误处理 |

**验收标准**：
- [ ] DeepSeek API正常调用
- [ ] SSE流式输出可用
- [ ] 对话上下文保持
- [ ] API错误优雅降级

#### 任务2.2：RAG知识库（1周）

| 子任务 | 文件 | 内容 |
|--------|------|------|
| 向量存储 | `w1_layer/knowledge_store.py` | 向量数据库集成 |
| 检索接口 | `w1_layer/memory.py` | 记忆层接入向量检索 |
| 知识导入 | `tools/knowledge_import.py` | 知识库导入工具 |
| RAG增强 | `m_layer/cognition.py` | 检索结果增强LLM输入 |

**验收标准**：
- [ ] 知识库可导入
- [ ] 向量检索准确率 ≥ 85%
- [ ] RAG增强回复质量提升
- [ ] 检索延迟 < 200ms

#### 任务2.3：13种工具补全（1周）

| 子任务 | 文件 | 内容 |
|--------|------|------|
| 工具实现 | `w1_layer/action.py` | 补全9种缺失工具 |
| tool_type标注 | `w1_layer/action.py` | 所有工具标注read/write |
| 工具守卫适配 | `guard/tool_guard.py` | 13种工具税率映射 |
| 沙箱限制 | `w1_layer/action.py` | file_read/file_write沙箱校验 |

**验收标准**：
- [ ] 13种工具全部可用
- [ ] tool_type标注完整
- [ ] 工具守卫税率正确
- [ ] 文件操作沙箱生效

#### 任务2.4：完整前端迁移（1周）

| 子任务 | 文件 | 内容 |
|--------|------|------|
| 前端迁移 | `web/index.html` | 迁移原130KB前端 |
| API适配 | `web/index.html` | 适配SCU3新API |
| 反馈按钮 | `web/index.html` | 点赞/点踩/分享/重试按钮 |
| 熵税展示 | `web/index.html` | 熵税消耗标签 |

**验收标准**：
- [ ] 前端页面正常加载
- [ ] API适配无报错
- [ ] 反馈按钮功能正常
- [ ] 熵税信息展示

#### 任务2.5：P0风险修复（并行）

| 风险 | 修复方案 | 文件 |
|------|---------|------|
| 保底余额滥用 | 限制补充频率（每日3次）+ 受限E点 | `w1_layer/ledger_runtime.py` |
| 内容过滤增强 | 白名单字段 + ML异常检测 | `guard/content_filter.py` |
| 白名单哈希校验 | 运行时校验代码一致性 | `guard/whitelist.py` |

**验收标准**：
- [ ] 保底余额每日限3次
- [ ] 内容过滤规则库50+条
- [ ] 白名单代码哈希校验生效

### 4.3 阶段2门槛

| 门槛项 | 标准 | 阻塞阶段3 |
|--------|------|-----------|
| 功能对等率 | ≥ 95% | ✅ 是 |
| 单元测试覆盖率 | ≥ 80% | ✅ 是 |
| 安全测试 | 0个CRITICAL | ✅ 是 |
| 性能基线 | P99 < 3s | ✅ 是 |

---

## 5. 阶段3：灰度迁移验证（3周）

### 5.1 目标

5%流量灰度验证，对比监控，异常回滚。

### 5.2 任务清单

#### 任务3.1：灰度路由（1周）

| 子任务 | 内容 |
|--------|------|
| 流量分流器 | 5%流量路由到SCU3，95%留原SCU_CUF |
| 用户标识 | 按user_id哈希分流，同一用户始终路由到同一系统 |
| 会话保持 | 灰度用户会话不中断 |

#### 任务3.2：对比监控（1周）

| 指标 | SCU3 | 原SCU_CUF |
|------|------|-----------|
| 响应延迟P99 | < 2s | 基线 |
| 错误率 | < 0.1% | 基线 |
| 熵税消耗 | 监控 | 监控 |
| 用户满意度 | 反馈净票数 | 反馈净票数 |
| 工具调用成功率 | ≥ 99% | 基线 |

#### 任务3.3：回滚演练（1周）

| 子任务 | 内容 |
|--------|------|
| 回滚脚本 | 一键回滚到原SCU_CUF |
| 数据迁移 | SCU3账本数据导出/导入 |
| 演练验证 | 模拟故障，验证回滚流程 |

### 5.3 阶段3门槛

| 门槛项 | 标准 | 阻塞阶段4 |
|--------|------|-----------|
| 灰度运行时长 | ≥ 7天 | ✅ 是 |
| 错误率 | < 0.1% | ✅ 是 |
| 响应延迟P99 | < 2s | ✅ 是 |
| 用户满意度 | 不低于原系统 | ✅ 是 |
| 回滚演练 | 通过 | ✅ 是 |

---

## 6. 阶段4：生产切换（1周）

### 6.1 目标

100%流量切换到SCU3，原SCU_CUF下线存档。

### 6.2 任务清单

| 子任务 | 内容 |
|--------|------|
| 全量切换 | 100%流量路由到SCU3 |
| 原系统下线 | 停止原SCU_CUF服务 |
| 数据归档 | 原SCU_CUF数据导出归档 |
| 运维移交 | SCU3运维文档 + 告警 + 监控 |
| 项目总结 | 实施总结报告 + 经验沉淀 |

---

## 7. 风险管控与回滚预案

### 7.1 风险管控矩阵

| 阶段 | 主要风险 | 概率 | 影响 | 缓解措施 |
|------|---------|------|------|---------|
| 阶段1 | 架构原则理解偏差 | 中 | 高 | 团队培训 + 代码审查 |
| 阶段2 | 功能补全延迟 | 中 | 中 | 优先级排序 + 增量交付 |
| 阶段2 | 性能不达标 | 低 | 高 | 性能基线 + 提前压测 |
| 阶段3 | 灰度异常 | 中 | 高 | 5%流量 + 实时监控 + 回滚 |
| 阶段4 | 切换故障 | 低 | 高 | 分批切换 + 回滚预案 |

### 7.2 回滚预案

```
触发条件：
  - 错误率 > 1%
  - 响应延迟P99 > 5s
  - 余额异常消耗
  - 安全告警

回滚步骤（< 5分钟）：
  1. 流量切回原SCU_CUF（DNS/路由切换）
  2. SCU3服务隔离（不停止，便于排查）
  3. 数据导出（SCU3账本 → 备份）
  4. 故障分析 + 修复 + 重新灰度

回滚验证：
  - 原SCU_CUF服务正常
  - 用户无感知
  - 数据无丢失
```

---

## 8. 验收标准清单

### 8.1 架构合规验收（5条原则）

| # | 原则 | 验收项 | 验证方式 |
|---|------|--------|---------|
| 1 | D层只放代码定义 | D层文件清单 + 哈希校验 + A1拒绝写D层 | 完整性校验脚本 |
| 2 | A4只管依赖方向 | 数据流豁免 + 依赖扫描0违规 | 依赖扫描器 + A4测试 |
| 3 | 同层流动免审 | 同层bypass日志 + 守卫点文档 | 日志审计 |
| 4 | 工具调用独立守卫 | 13种工具过ToolGuard + 白名单归档 | 工具调用测试 |
| 5 | 输出必经内容过滤 | ContentFilter强制调用 + 50+规则 | 输出审计 |

### 8.2 功能对等验收

| # | 功能 | SCU3 | 原SCU_CUF | 对等标准 |
|---|------|------|-----------|---------|
| 1 | LLM对话 | DeepSeek接入 | DeepSeek接入 | 回复质量相当 |
| 2 | RAG增强 | 向量检索 | 向量检索 | 检索准确率 ≥ 85% |
| 3 | 工具调用 | 13种 | 13种 | 全部可用 |
| 4 | 反馈系统 | 点赞/点踩 | 无（已移除） | SCU3更优 |
| 5 | 前端 | 完整 | 完整 | 功能对等 |
| 6 | 熵税系统 | v3 | v1 | SCU3更优 |

### 8.3 性能验收

| 指标 | 标准 | 验证方式 |
|------|------|---------|
| P99响应延迟 | < 2s | 压测 |
| 1000 QPS稳定性 | 持续30分钟无错误 | 压测 |
| 熵税计算延迟 | < 10ms | 基准测试 |
| 内存占用 | < 512MB | 监控 |
| 启动时间 | < 5s | 计时 |

### 8.4 安全验收

| # | 安全项 | 标准 | 验证方式 |
|---|--------|------|---------|
| 1 | 公理合规 | 0个A1-A4违规 | 安全测试 |
| 2 | 注入防护 | SQL/XSS/路径穿越0漏洞 | 渗透测试 |
| 3 | 越权防护 | 文件沙箱 + 路径校验 | 越权测试 |
| 4 | 信息泄漏 | 内容过滤0泄漏 | 泄漏测试 |
| 5 | 并发安全 | 10万并发无竞态 | 压测 |

---

## 附录：实施时间线

```
Week 1-2:  阶段1 - 架构基线确立
           ├─ D层固化 (3天)
           ├─ A4强化 (2天)
           ├─ 守卫完善 (3天)
           ├─ 内容过滤 (2天)
           └─ 测试框架 (4天)

Week 3-6:  阶段2 - 核心功能补全
           ├─ DeepSeek接入 (1周)
           ├─ RAG知识库 (1周)
           ├─ 13种工具 (1周)
           ├─ 前端迁移 (1周)
           └─ P0风险修复 (并行)

Week 7-9:  阶段3 - 灰度迁移验证
           ├─ 灰度路由 (1周)
           ├─ 对比监控 (1周)
           └─ 回滚演练 (1周)

Week 10:   阶段4 - 生产切换
           ├─ 全量切换
           ├─ 原系统下线
           └─ 运维移交
```

---

**文档状态**: 最终版
**生成时间**: 2026-08-10
**适用范围**: v3架构落地实施指导
